<div class="card">
  <%= form_with model: [@book, @review] do |f| %>

    <label>Content</label>
    <%= f.text_area :content %>

    <label>Rating</label>
    <%= f.number_field :rating %>

    <%= f.submit "Submit", class: "btn" %>

  <% end %>
</div>